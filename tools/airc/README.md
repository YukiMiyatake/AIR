# AIR Phase 1 reference CLI (`airc`)

TypeScript / Node implementation of the bootstrap toolchain.
See [docs/PHASE1_DECISIONS.md](../../docs/PHASE1_DECISIONS.md) and [docs/SUBSET.md](../../docs/SUBSET.md).

```bash
npm install
npm run build
npm run airc -- version
npm run airc -- check examples/sum.air.json
```

Phase 1 status: scaffold (parse/check/run land in follow-up PRs).
